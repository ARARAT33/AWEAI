# Deduplication

Deduplication removes duplicate records from datasets.

## Usage

```python
from aweai.data.deduplication import Deduplicator

dedup = Deduplicator()
X_dedup = dedup.remove_duplicates(X)
```

## Related Pages

- [Cleaning](Cleaning.md) — Data cleaning
- [Validation](Validation.md) — Data validation
