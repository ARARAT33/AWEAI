# Overview

Database layer for metadata, vectors, time-series, graphs and KV.

## Overview

Database layer for metadata, vectors, time-series, graphs and KV.

## Usage

```bash
aweai db init
aweai db backup
aweai db vector search --query 'machine learning'
```
