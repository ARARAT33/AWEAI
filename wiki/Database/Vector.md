# Vector

Vector database for RAG and similarity search.

## Overview

Vector database for RAG and similarity search.

## Usage

```bash
aweai db init
aweai db backup
aweai db vector search --query 'machine learning'
```
