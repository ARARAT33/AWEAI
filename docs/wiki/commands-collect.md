# collect commands

Total commands: **14**

| Command | Description |
| --- | --- |
| `scrape` | Scrape a URL and extract readable text. |
| `crawl` | Crawl a list of seed URLs, extract text, save each page. |
| `fetch-json` | Fetch JSON from an API/URL. |
| `rss` | Parse an RSS/Atom feed into entries. |
| `import-csv` | Import CSV into JSONL (one row per line). |
| `import-json` | Import JSON (array) into JSONL. |
| `export-csv` | Export JSONL to CSV (union of keys). |
| `dedupe` | Remove duplicate rows from JSONL. |
| `sample` | Randomly sample rows from JSONL. |
| `shuffle` | Shuffle rows of a JSONL file. |
| `clean` | Clean a text file (HTML strip, whitespace, URLs, emails). |
| `augment-text` | Augment text with synonym swaps, shuffles and noise (simple). |
| `synthetic` | Generate synthetic data (text/jsonl/numbers/csv). |
| `stats` | Show basic dataset stats (rows, chars, unique). |
